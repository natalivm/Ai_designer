// Full readable lesson content for each chapter, keyed by block id.
// Shape per chapter:
//   { intro: string, readingTime: number,
//     lessons: [{ title, paragraphs: string[], bullets?: string[], tip?: string }],
//     practice?: string }
// Chapters without an entry here fall back to their topic list in the reader.
export const CHAPTER_CONTENT = {}
